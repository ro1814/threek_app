import React from 'react'
import { Container, Row, Col } from 'react-bootstrap'

const Retador = (props) => {
    return <card-retador>
            <Container>
                <div>
                    <img src={props.imag}></img>
                </div>
            </Container>
        </card-retador>
    
}

export default Retador
